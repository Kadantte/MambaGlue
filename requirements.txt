torch>=2.1.0
torchvision>=0.16.0
numpy
opencv-python
matplotlib
kornia>=0.6.11
einops
omegaconf
requests
# Note: mamba-ssm is intentionally NOT pinned here - install it from source
# (see README: `git clone https://github.com/state-spaces/mamba && pip install .`)

